# RPGFramework.DI
Lightweight DI system for Unity

Injects via constructor only (for now)

Has both a global installer and scene only installers.  Global installers are intended to last the lifetime of the application, scene installers should overwrite the current fallback on scene change

Samples to come
